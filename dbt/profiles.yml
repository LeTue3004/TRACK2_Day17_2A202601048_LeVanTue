lab17:
  target: dev
  outputs:
    dev:
      type: duckdb
      # Makefile / run_pipeline.py luôn export LAB17_DB = <repo>/warehouse.duckdb
      path: "{{ env_var('LAB17_DB', '../warehouse.duckdb') }}"
      threads: 1
      retries:
        query_attempts: 3
